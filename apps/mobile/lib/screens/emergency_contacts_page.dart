import 'contacts_page.dart';

/// Compatibility route: all contact entry points share the unified screen.
class EmergencyContactsPage extends ContactsPage {
  const EmergencyContactsPage({super.key, super.imei, super.wearerName, super.service});
}
