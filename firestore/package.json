{
  "name": "guardian-firestore-rules",
  "version": "0.1.0",
  "private": true,
  "description": "Emulator tests for Guardian Firestore authorization boundaries",
  "scripts": {
    "test": "firebase --config firebase.test.json emulators:exec --project guardian-rules-test --only firestore \"node --test test/*.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.17.1",
    "firebase-tools": "^15.27.0"
  }
}
